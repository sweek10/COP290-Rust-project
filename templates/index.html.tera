<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spreadsheet</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        table {
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ccc;
            padding: 8px;
            text-align: center;
            min-width: 60px;
        }
        th {
            background-color: #f0f0f0;
        }
        .row-number {
            width: 50px;
            background-color: #f0f0f0;
        }
        .bold {
            font-weight: bold;
        }
        .italic {
            font-style: italic;
        }
        .underline {
            text-decoration: underline;
        }
        .controls {
            margin-bottom: 20px;
        }
        button {
            margin-right: 10px;
            padding: 8px 16px;
        }
        input[type="text"] {
            padding: 8px;
            width: 300px;
        }
        .status {
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="status">
        Status: {% if circular_detected %}(err){% else %}(ok){% endif %}
    </div>
    <div class="controls">
        <form action="/scroll/w" method="post">
            <button type="submit">Up</button>
        </form>
        <form action="/scroll/s" method="post">
            <button type="submit">Down</button>
        </form>
        <form action="/scroll/a" method="post">
            <button type="submit">Left</button>
        </form>
        <form action="/scroll/d" method="post">
            <button type="submit">Right</button>
        </form>
    </div>
    <table>
        <tr>
            <th></th>
            {% for col in columns %}
                <th>{{ col }}</th>
            {% endfor %}
        </tr>
        {% for row in rows %}
            <tr>
                <th class="row-number">{{ row.number }}</th>
                {% for cell in row.cells %}
                    <td class="{{ cell.classes }}">{{ cell.value }}</td>
                {% endfor %}
            </tr>
        {% endfor %}
    </table>
    <form action="/command" method="post">
        <input type="text" name="command" placeholder="Enter command (e.g., A1=5, FORMULA A1)">
        <button type="submit">Execute</button>
    </form>
</body>
</html>